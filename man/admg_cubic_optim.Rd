% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/admg-cubic.R
\name{admg_cubic_optim}
\alias{admg_cubic_optim}
\title{Optimal Learning of ADMGs via Mixed Integer Programming with Cubic Encoding}
\usage{
admg_cubic_optim(
  tests,
  d = 3,
  max_size = d - 2,
  V = letters[1:d],
  trafo = function(x) as.numeric(x <= 0.05),
  weight_type = c("const", "inv", "log", "size"),
  warmstart = NULL,
  edgehints = NULL,
  gurobi_args = list(),
  verbose = FALSE,
  cache = TRUE,
  cache_dir = "./.cache-admg-cubic-redundant",
  mode = c("admg", "dmg"),
  ...
)
}
\arguments{
\item{tests}{A data frame or list of conditional independence test results.}

\item{d}{Integer. Number of variables/nodes.}

\item{max_size}{Integer. Maximum size of conditioning set.}

\item{V}{Character vector of variable names, of length \code{d}.}

\item{trafo}{Transformation function applied to test p-values (default:
\code{function(x) as.numeric(x <= 0.05)}).}

\item{weight_type}{Character. Type of weighting ("const", "inv", "log", "size").}

\item{warmstart}{Optional. Matrix, list, or NULL. Used to warm-start optimization.}

\item{edgehints}{Optional. Edge hints to speed up or constrain the solution.}

\item{gurobi_args}{List of arguments passed to the \code{gurobi} solver.}

\item{verbose}{Logical. If \code{TRUE}, print progress messages.}

\item{cache}{Logical. If \code{TRUE}, cache constraint matrices and results.}

\item{cache_dir}{Character. Directory where cache files are saved.}

\item{mode}{Character. Learning mode: "admg" (default) or "dmg".}

\item{...}{Additional arguments passed to internals.}
}
\value{
An object of class \code{graphopt} with components:
\describe{
\item{graph}{Adjacency or edge matrices of the learned graph.}
\item{tests}{Test results with optimization variables.}
\item{optim}{Solver output.}
}
}
\description{
Solves a global mixed integer optimization problem to learn an Acyclic
Directed Mixed Graph (ADMG) or DMG from supplied conditional independence
test results, using the \code{gurobi} solver. Supports warm-start, different
weight types, and optional caching for efficiency. This cubic variant
focuses on a redundant but faster encoding.
}
\details{
Requires the \code{gurobi} package. Will warn if unavailable.
}
